#view_userprofilesettings
  br
  br
  
  - if (usersession.isLoggedIn())
    a.edit-profile.js-edit-profile(title='Edit your profile')
      = "Edit profile"
    br
    br
      
    a.update-avatar.js-update-avatar(title='Update your avatar')
      = "Update avatar"
      
    br
    br

  a.create-profile.js-create-profile(title='Create a new user profile')
    = "Create profile"
    
  - if (usersession.isLoggedIn() && user.admin())
    br
    br
    a.verify-user.js-verify-user(title="Update user's verification flag")
      = "Verify users(Todo)"
  
  br
  br
  - if (usersession.isLoggedIn())
    a.logout.js-logout(title='Logout')
      = "Logout"   
